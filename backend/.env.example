# Database (PostgreSQL)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=warignan
DATABASE_PASSWORD=warignan_secret
DATABASE_NAME=warignan_db

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# App
PORT=3000
NODE_ENV=development

# Upload (dossier pour preuves paiement)
UPLOAD_DIR=./uploads

# CORS (frontend URL)
CORS_ORIGIN=http://localhost:5173
