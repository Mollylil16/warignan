import { Application } from "express";

export function registerRoutes(app: Application) {
  // Ici on branchera les routes par ressource (products, users, orders, etc.)
  // Exemple:
  // app.use("/products", productsRouter);
}

